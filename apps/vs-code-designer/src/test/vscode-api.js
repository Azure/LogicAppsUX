/**
 * This is an empty placeholder file that will be replaced at runtime
 * with a module that exports the actual VS Code API from the extension host.
 */

// Default export is an empty object if not running in VS Code extension host
export default {};
