import { makeStyles } from '@fluentui/react-components';

export const useConnectionSelectionStyles = makeStyles({
  container: {
    width: 'calc(90%)',
    padding: '16px 0 0 16px',
  },
  loadingContainer: {
    paddingTop: '50px',
  },
});
