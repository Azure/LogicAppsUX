import type { Size } from '@microsoft/logic-apps-shared';
import type { Node } from '@xyflow/react';
export declare const DesignerFlowViewPadding: number;
export declare const getTargetPositionForWorkflow: (firstNode: Node, windowSize: Size, defaultNodeSize: Size) => [number, number];
