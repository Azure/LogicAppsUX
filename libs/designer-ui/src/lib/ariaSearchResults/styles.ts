import { makeStyles } from '@fluentui/react-components';

export const useAriaSearchResultsStyles = makeStyles({
  root: {
    width: '1px',
    height: '1px',
    margin: '-1px',
    opacity: 0,
  },
});
