import { makeStyles } from '@fluentui/react-components';

export const useMockStatusIconStyles = makeStyles({
  mockStatusIcon: {
    position: 'absolute',
    right: '-6px',
    top: '-6px',
    zIndex: 10,
  },
});
