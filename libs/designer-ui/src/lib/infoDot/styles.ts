import { makeStyles } from '@fluentui/react-components';

export const useInfoDotStyles = makeStyles({
  root: {
    width: '16px',
  },
  tooltipContent: {
    display: 'flex',
    flexDirection: 'column',
    gap: '8px',
  },
});
