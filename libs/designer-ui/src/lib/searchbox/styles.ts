import { makeStyles } from '@fluentui/react-components';

export const useSearchBoxStyles = makeStyles({
  root: {
    // Base styles for search box
    // Note: margin is handled by parent containers like .msla-sub-heading-container
  },
});
