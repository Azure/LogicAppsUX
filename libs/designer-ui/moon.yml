id: designer-ui
language: typescript
platform: node
tags: []

tasks:
  build:
    outputs:
      - '/dist/rollup/designer-ui/'
  yalcpush:
    command: npm run yalcpush
    deps:
      - ~:build
    outputs:
      - '{options.outputPath}'
type: library
