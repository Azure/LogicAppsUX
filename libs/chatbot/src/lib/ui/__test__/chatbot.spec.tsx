import { describe, vi, beforeEach, afterEach, beforeAll, afterAll, it, test, expect } from 'vitest';
describe('chatbot', () => {
  it('should work', () => {
    // single test to allow tests to run. We should add tests
    expect(true).toBeTruthy();
  });
});
